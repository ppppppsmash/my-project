version: '3'
services:
  client:
    build: ./docker/client/
    ports:
      - '9999:3000'
    volumes:
      - ./client:/app
    command: bash
    tty: true
    environment:
      - CHOKIDAR_USEPOLLING=true
